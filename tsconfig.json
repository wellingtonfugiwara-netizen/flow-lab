{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    // TS 6 nao inclui mais @types/* sozinho: sem isto, describe/it/expect
    // somem no typecheck (os testes rodam, o tsc reprova).
    "types": ["jest", "node"],
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
